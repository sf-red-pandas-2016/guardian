<h1>All my events</h1>

<ul>
  <% @events.each do |event| %>
  <% date = event.created_at %>
  <% formatted_date = date.strftime("%a %B %d") %>
    <li><%= link_to formatted_date,  event_path(event), {class:"#"} %></li>
  <%end%>
</ul>
